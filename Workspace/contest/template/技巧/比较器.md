
```cpp

class Compare{
	public:
		bool operator() (arr2 a, arr2 b) const {
            
		}
};



auto cmp = [](const Node& a, const Node& b) {
    if (a.x != b.x) return a.x > b.x;
    return a.y > b.y;
};

priority_queue<Node, vector<Node>, decltype(cmp)> pq(cmp);

```