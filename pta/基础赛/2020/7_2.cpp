//7_2

#include<stdio.h>

int main()
{
	int a, b, c;
	scanf("%d %d %d", &a, &b, &c);
	
	if(a+b>=c){
		printf("It's not safe!GG!");
	}else{
		printf("It's too safe,isn't it?");
	}
	
	return 0;
}
